language: python

python:
    - "2.7"
    - "pypy-5.3.1"

env:
    - TWISTED="latest"
    - TWISTED="trunk"
    - TWISTED="16.2"
    - TWISTED="16.0"
    - TWISTED="15.0"
    - TWISTED="14.0"
    - FLAKES="pyflakes"
    - MANIFEST="manifest"

matrix:
  fast_finish: true
#  allow_failures:
#    - env: TOX_ENV=pyflakes

install:
  - pip install tox-travis coveralls

script: tox

after_success:
    - coveralls

notifications:
    email: false
